name: PR Hygiene

on: [pull_request]

jobs:
  fork-local-files:
    runs-on: ubuntu-24.04

    steps:
    - uses: actions/checkout@v4

    - name: Reject fork-local planning files
      run: python3 .github/scripts/hygiene.py plans
